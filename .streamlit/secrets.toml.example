# Copy this file to .streamlit/secrets.toml (gitignored) and fill in values.
# Only needed when migrating from DuckDB to BigQuery.

# --- BigQuery credentials (optional) ---
# [bigquery]
# project_id = "your-gcp-project-id"
# dataset   = "your_dataset_name"
# table     = "telco_customers"
# Credentials: set GOOGLE_APPLICATION_CREDENTIALS env var
# or paste a service account JSON key below:
# credentials_json = """{ ... }"""
